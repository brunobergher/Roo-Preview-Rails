<div id="<%= dom_id(testimonial) %>" class="break-inside-avoid bg-white rounded-lg shadow p-5 border-l-4 border-red-500">
  <p class="text-gray-700 text-sm mb-3"><%= testimonial.body %></p>
  <div class="flex items-center justify-between">
    <span class="font-semibold text-sm text-gray-800"><%= testimonial.name %></span>
    <span class="text-xs text-gray-400"><%= time_ago_in_words(testimonial.created_at) %> ago</span>
  </div>
</div>
